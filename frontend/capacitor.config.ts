import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'style.oxygen.app',
  appName: '氧气捏捏',
  webDir: 'dist',
  backgroundColor: '#F4F7F2',
}

export default config
